const initialState = {}
const a = "a"
const rootReducer = (state = initialState, action) => {
  switch (action.type) {

  case a :
    return 

  default:
    return state
  }
}

export default rootReducer;
